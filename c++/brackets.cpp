#include <iostream>
#include <string.h>
using namespace std;

char c[100100];

int main(){
    cin>>c;
    int n;
    //for(n=0; c[n]!='\0'; ++n);
    n = strlen(c);
    for (int i=0; c[i]!='\0'; i++){
        if (c[i]=='('){

        }
        if (c[i]==')'){

        }
    }
    return 0;
}
